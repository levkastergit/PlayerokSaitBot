# HTTP: прокси на Node на хосте (порт из backend/.env PORT, по умолчанию 3000).
upstream playerok_backend {
    server host.docker.internal:3000;
}

server {
    listen 80;
    listen [::]:80;
    server_name _;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://playerok_backend;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
    }
}

# Если сертификаты уже в volume certbot-conf, раскомментируйте блок и подставьте домены.
# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name wesqaliqo.com;
#     ssl_certificate /etc/letsencrypt/live/wesqaliqo.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/wesqaliqo.com/privkey.pem;
#     location / {
#         proxy_pass http://playerok_backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#     }
# }
